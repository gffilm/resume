# Enable URL rewriting
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect to /resume/index.html
    RewriteRule ^resume$ /resume/index.html [L]

    # Handle 404 errors
    ErrorDocument 404 /404.html
</IfModule>
